{
  "name": "zoomphone-ghl-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that mirrors Zoom Phone call activity into GoHighLevel.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
