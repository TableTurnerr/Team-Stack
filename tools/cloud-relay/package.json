{
  "name": "@tableturnerr/cloud-relay",
  "version": "1.0",
  "private": true,
  "type": "module",
  "description": "Always-up store-and-forward relay for Zoom webhooks and agent recording uploads. Buffers in R2 while the home server is down, drains back home when it returns, keeps nothing after handoff.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.16.0"
  }
}
